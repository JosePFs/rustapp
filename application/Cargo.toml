[package]
name = "application"
version.workspace = true
edition.workspace = true

[dependencies]
common = { path = "../common" }
domain = { path = "../domain" }
async-trait = "0.1"
chrono = "0.4"
futures = "0.3"
log = "0.4"

[dev-dependencies]
tokio = { version = "1", features = ["macros", "rt"] }
