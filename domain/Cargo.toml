[package]
name = "domain"
version.workspace = true
edition.workspace = true

[dependencies]
common = { path = "../common" }
async-trait = "0.1"
uuid = { version = "1", features = ["v4"] }
regex = "1"
